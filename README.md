Tutorial Exercises from The Rust Book
====

The virtual workspace `Cargo.toml` in this directory lists the currently available projects. There are debug targets for each in the `.vscode/launch.json` config.

